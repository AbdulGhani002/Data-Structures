#include<iostream>
#include"List.h"
using namespace std;

int main(void){
  Node node1, node2;
  node1.data = 1;
  node2.data = 2;

  LinkedList ll;
  ll.init(node1);
  ll.insert_at_head(node2);
  ll.display();
}
